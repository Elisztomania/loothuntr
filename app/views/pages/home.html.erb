<div class="container">
  <h1>Pages#home</h1>
  <p>Find me in app/views/pages/home.html.erb</p>

  <% @quests.each do |quest| %>
    <%= quest.title %>
    <%= quest.id %>
  <% end %>

  <%= @guild.name %>
  <%= @guild.id %>

  <%= current_user != nil ? @current_user.username : "" %>

  <%= link_to "Voir toutes les quêtes", quests_path %>
</div>
