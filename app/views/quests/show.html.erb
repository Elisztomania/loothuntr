

<!--Test-->

<!--Header-->
<div class="font-sans leading-normal tracking-normal">
    <div class="w-full m-0 p-0 bg-cover bg-bottom" style="background-image:url('<%= cl_image_path @quest.photo.key %>'); height: 60vh; max-height:460px; box-shadow: inset 0 0 0 100vw rgba(7, 25, 43, 0.4)">
      <div class="container max-w-4xl mx-auto pt-16 md:pt-32 text-center break-normal">
        <!--Title-->
          <p class="text-white font-extrabold text-3xl md:text-5xl">
            🤠 <%= @quest.title %>
          </p>
      </div>
    </div>

    <!--Container-->
    <div class="container px-4 md:px-0 max-w-6xl mx-auto -mt-32">
      <div class="mx-0 sm:mx-6">
        <div class="bg-white-200 w-full text-xl md:text-2xl text-gray-800 leading-normal rounded-t">

        <!--Lead Card-->
        <div class="flex h-full bg-white rounded overflow-hidden shadow-lg">

            <div class="w-full md:w-1/2 rounded-t group transition-transform duration-200 transform sm:hover:-translate-y-1">
              <div class="flex-1 bg-white rounded-t rounded-b-none overflow-hidden shadow-lg h-full">
                <div class="w-full font-bold text-xl text-gray-900 px-6 mt-4 mb-4">🤔 Description</div>
                <p class="text-gray-800 font-serif text-base px-10 mb-5 ">
                  <%= @quest.description %>
                </p>
              </div>
            </div>

            <div class="w-full md:w-1/2 rounded-t group transition-transform duration-200 transform sm:hover:-translate-y-1">
              <div class="flex-1 bg-white rounded-t rounded-b-none overflow-hidden shadow-lg h-full">
                <div class="w-full font-bold text-xl text-gray-900 px-6 mt-4 mb-4 ">🤩 Informations clés</div>
                  <div class="text-gray-800 font-serif text-base px-10 justify-self-center">
                     <p> 📍 <%= @quest.location %> </p>

                     <p> ➡️ <%= @quest.resolved ? "Résolue" : "Non résolue" %> </p>

                     <p> 🕺 <%= @quest.creator.username %> </p>

                     <p> Guilde active sur la quête : <%= @quest.guild.name %> </p>

                  <div class="text-right place-self-end">
                    <% if current_user != nil && current_user == @quest.creator %>
                      <%= link_to edit_quest_path(@quest.id) do %>
                        <i class="fas fa-pencil-alt"></i>
                      <% end %>
                    <% end %>
                  </div>

                  </div>


              </div>
            </div>

        </div>
      </div>
    </div>

<!--  INFO SI GUILDE ET LA REJOINDRE / TABLEAU DE BORD -->
    <div class="mx-40 my-6">
      <% if @quest.guild.present? %>
        <% if current_user != nil && current_user.is_member_of?(@quest.guild) %>
          <%= link_to "Tableau de bord de guilde", guild_path(@quest.guild), class: "btn btn-warning" %>
        <% elsif current_user != nil && !current_user.has_apply_to?(@quest.guild) %>
          <%= link_to "Postuler pour les rejoindre", new_guild_member_path(@quest.guild), class: "btn btn-success" %>
        <% end %>
      <% elsif @quest.creator == current_user %>
          <%= link_to 'Créer une guilde', new_quest_guild_path(@quest), class: "" %>

      <% else %>
          <p>Pas de guilde active sur la quête</p>
      <% end %>
    </div>

<!-- POST DES AVANCEES BOUCLE-->
    <% @quest.avancees.each do |avancee| %>
    <div class="m-10 p-10">

      <strong><%= avancee.title %></strong>
      <p><%= avancee.description %></p>

      <div class="inline-flex space-x-4 p-6 center">
      <% avancee.photos.each do |photo| %>
          <div class="flex-1 self-center"><%= cl_image_tag photo.key, height: 300, width: 400, crop: :fill %></div>
      <% end %>
      </div>
    </div>
    <% end %>

  </div>


<!-- Code de la team BACK -->

<br>



<br>

<!-- RETOUR TOUTES LES QUESTS -->
  <%= link_to 'Retour', quests_path, class: "btn btn-primary mt-2" %>

</div>
<!-- Code de la team BACK -->
